import { PlaceholderShell } from "@/components/landing/PlaceholderShell";

export default function DonatePage() {
  return (
    <PlaceholderShell title="Donate">
      <p>
        Thanks for considering support. Donations are not live yet, but this page will be updated when that option is
        available.
      </p>
    </PlaceholderShell>
  );
}
